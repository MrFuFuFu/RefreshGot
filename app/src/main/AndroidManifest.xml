<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mrfu.swiperefreshboth" >

    <application
        android:name="mrfu.swiperefreshboth.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="mrfu.swiperefreshboth.RecyclerActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="mrfu.swiperefreshboth.GridActivity" />
        <activity android:name="mrfu.swiperefreshboth.ListViewDemoActivity" />
        <activity android:name="mrfu.swiperefreshboth.TestActivity"/>
    </application>

</manifest>
